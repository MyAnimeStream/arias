version: "3"

services:
  arias:
    container_name: Arias
    build: .
    image: myanimestream/arias
    restart: always
    networks:
      - internal
    environment:
      -
    volumes:
      - downloads:/downloads

networks:
  internal: {}

volumes:
  downloads: {}